import { createAuthClient } from "better-auth/react"
export const authClient = createAuthClient(
    // if we are in the same url as the client and the server then we do not need to pass anything along it.
)
